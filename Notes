Имена команд можно писать в любом регистре, т.к. используется UpperCase() для имём команд
Можно использовать многострочные комментарии, начинать строку символами /*, заканчивать */, причём эти символы должны быть первыми в строке
Можно подключать свои файлы конфигурации с помощью команды include путь_к_файлу, пока работает только в режиме релиза (макрос __DEBUG должен быть выключен)
